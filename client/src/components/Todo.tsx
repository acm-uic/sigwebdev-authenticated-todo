import React, { FC } from 'react'

// * Export Header Component
export const Todo: FC = () => {
    return (
        <div className="todo">
            {
                // TODO:
                // * Write ToDo component
            }
        </div>
    )
}
