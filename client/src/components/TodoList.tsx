import React, { FC } from 'react'

// * Export Header Component
export const TodoList: FC = () => {
    return (
        <div className="todoList">
            {
                // TODO:
                // * Write TodoList component
            }
        </div>
    )
}
